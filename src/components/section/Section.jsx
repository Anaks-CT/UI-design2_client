import SideBar from "../SideBar"
import SectionHeader from "./SectionHeader"

const Section = () => {
  return (
    <div className="flex">
        <SideBar />
        <SectionHeader />
    </div>
  )
}

export default Section